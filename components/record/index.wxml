<view class="container">
	<text class="date">{{data.date}} 支出：-{{data.consume}}，收入：+{{data.income}}</text>
	<view class="item-container" wx:for="{{data.items}}" wx:key="*this">
		<record-item data="{{item}}" />
	</view>

</view>